<%= simple_form_for(@attribute_layer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
	  <%= f.input :name %>
  </div>
  
  <%= f.fields_for :attributes do |a_form| %>
  <h1>Hello</h1>
  <% end %>
  <div class="form-actions">
    <%= f.button :submit, "Add" %>
  </div>
<% end %>
