<%= simple_form_for(@palette) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
	  <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, label: "Save and continue" %>
  </div>
<% end %>
